function compareArrays(arr1, arr2) {
  let result;

  arr1=[1,2,3];
  arr2=[1,2,4];
  console.log(JSON.stringify(arr1)==JSON.stringify(arr2));

  return result; // boolean
}
compareArrays();//без этого ни как не могу

function advancedFilter(arr) {
  let resultArr;

  

  return resultArr; // array
}
